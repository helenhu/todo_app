<% provide(:title, @user.name) %>
<h1><%= @user.name %></h1>

<div class="row">
	<div class="col-md-6 col-md-offset-3">
		<section class="todo_list">	
			<% if @user.tasks.any? %>
				<%= render @tasks %>
			<% end %>
		</section>
	</div>
</div>